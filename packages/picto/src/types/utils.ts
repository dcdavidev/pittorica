// Utility types
import { ColorKey } from './colors';
import { Radius } from './radius';

export type BorderBox = {
  borderWidth: string;
  borderColor: ColorKey;
  borderRadius: Radius;
};

export type ResetMargins = {
  margin: string;
};
